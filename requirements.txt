# Python 3.12 recommended 

Django>=5.2,<6.0
requests>=2.0
bleach>=6.0
python-dateutil>=2.8
pytest>=8.0
pytest-django>=4.0
python-dotenv>=1.0
celery>=5.4,<6.0

# Production dependencies for Render deployment
gunicorn>=26.2.0
psycopg2-binary>=2.9
whitenoise>=6.6
dj-database-url>=2.1
django-storages>=1.14
boto3>=1.34
sentry-sdk>=2.0

# Cache backend (optional, fallback to LocMemCache if not available)
django-redis>=5.4
redis>=5.0
hiredis>=2.0  # For better Redis parser performance

# Audio generation from MXL (MusicXML)
music21>=9.1
midi2audio>=0.1.1
pydub>=0.25.1  # WAV to MP3 conversion
mutagen>=1.47.0  # MP3 validation (check duration, bitrate, integrity)

# Developer tools
djlint>=1.36.0  # Django HTML formatter

# Import scripts dependencies
google-genai>=1.0
pymupdf>=1.24
